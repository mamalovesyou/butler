apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-octopus-secrets
type: Opaque
data:
  {{- range $key, $val := .Values.services.octopus.config.secrets }}
  {{ $key }}: {{ $val | b64enc }}
  {{- end}}

